#pragma once

#include"Structure.h"
#include<iostream>
#include<string>

using namespace std;

double getNumber(string str,int *from);

string getString(string str,int *from,bool *var);

void getWord(Expression*);
